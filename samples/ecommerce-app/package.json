{
  "name": "ecommerce-cosmosdb-sample",
  "version": "1.0.0",
  "description": "E-commerce sample application with Azure Cosmos DB",
  "main": "src/backend/server.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd src/backend && nodemon server.js",
    "dev:frontend": "cd src/frontend && npm start",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd src/backend && npm run build",
    "build:frontend": "cd src/frontend && npm run build",
    "start": "cd src/backend && npm start",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd src/backend && npm test",
    "test:frontend": "cd src/frontend && npm test",
    "setup": "npm install && cd src/backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "azure",
    "cosmosdb",
    "ecommerce",
    "sample",
    "nodejs",
    "react"
  ],
  "author": "Microsoft",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}